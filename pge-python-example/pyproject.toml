[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pge-python-example"
version = "0.1.0"
description = "EPIC Python example app"
requires-python = ">=3.11"

