version: 0.2
env:
  exported-variables:
    - GIT_HASH
    - WIZ_REPORT
  secrets-manager:
    GITHUB_TOKEN: "system/github"
  parameter-store:
    WIZ_PROJECT_UUID: "/lm/wiz_project_uuid"
phases:
  install:
    commands:
      - echo "Installing WIZ CLI"
      - echo "$ENVIRONMENT"
      - echo "$APP_OWNERS"

      - mkdir -p /tmp/wizcli
      - curl -o /tmp/wizcli/wizcli https://downloads.wiz.io/wizcli/latest/wizcli-linux-amd64
      - chmod +x /tmp/wizcli/wizcli
      - /tmp/wizcli/wizcli version
      - /tmp/wizcli/wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"

      - echo "Install JFROG CLI"
      - curl -k -fL https://install-cli.jfrog.io | sh && jf --version

      - echo "Getting ECR credentials"
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com
  pre_build:
    commands:
      - GIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -b -8)

  build:
    commands:
      - echo "Entered the install phase..."
      - echo $(date +%Y-%m-%d)
      - cd ${build_path}

      - echo "Building image ..."
      - docker buildx build -t $ECR_REGISTRY:$GIT_HASH --secret id=github_pat,env=GITHUB_TOKEN .

      - echo Pushing image '$ECR_REGISTRY:$GIT_HASH'
      - docker push $ECR_REGISTRY:$GIT_HASH

      - echo Scanning image with WIZ
      - WIZ_RESULTS=$(/tmp/wizcli//wizcli docker scan -d mountWithLayers --file-hashes-scan --image $ECR_REGISTRY:$GIT_HASH --project $WIZ_PROJECT_UUID)
      - echo $WIZ_RESULTS

      - WIZ_REPORT=$(echo $WIZ_RESULTS | grep -Po "(?<=Scan report:)\s*[^\s]*" | xargs)
      - echo $WIZ_REPORT

      - echo **********  Successfully Executed ***************

# Include only the files required for your application to run.
# Do not use recursively include artifacts from node_modules directory as it will include unnecessary packages
# ExpressJS apps will need other artifact directories included (bin/*, public/*, routes/*, views/* etc).
artifacts:
  files:
    - "**/*"
