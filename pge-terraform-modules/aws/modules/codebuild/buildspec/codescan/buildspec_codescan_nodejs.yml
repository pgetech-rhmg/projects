# Do not change version. This is the version of aws buildspec, not the version of your buldspec file.
version: 0.2
env:
  exported-variables:
    - PROJECT_NAME
    - PROJECT_ROOT_DIRECTORY

  variables:
    SONAR_TOKEN                : "${SONAR_TOKEN}"
    SONAR_HOST                 : "${SONAR_HOST}"
    ARTIFACTORY_TOKEN          : "${ARTIFACTORY_TOKEN}"
    GIT_REPO_BRANCH            : "${GIT_REPO_BRANCH}"
    PROJECT_NAME               : "${PROJECT_NAME}"
    SONAR_SCANNER_CLI_VERSION  : "${SONAR_SCANNER_CLI_VERSION}"

phases:
  install:
    runtime-versions:
      nodejs: $NODEJS_VERSION_CODESCAN
    commands:
      - echo "Entered the installation phase..."
      - echo "codebuild app language - $CODEBUILDAPP_LANGUAGE"
      - echo "nodejs runtime - $NODEJS_VERSION_CODESCAN"
      - echo "Sonar-scanner CLI download URL - $SONAR_SCANNER_CLI_VERSION"

      - echo "Entered the installation phase..."
      - echo "Install JFROG CLI"
      - curl -k -fL https://install-cli.jfrog.io | sh && jf --version

      - echo "Installing Sonar Scanner CLI"
      - wget "https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-${SONAR_SCANNER_CLI_VERSION}-linux.zip"
      - filename="sonar-scanner-cli-${SONAR_SCANNER_CLI_VERSION}-linux.zip"
      - folder_name=$(unzip -l $filename | head -n 4 | tail -n 1 | awk '{print $4}')
      - echo $filename && echo $folder_name
      - unzip -o $filename -d /var/tmp/ && rm -rf $filename
      - export PATH=/var/tmp/$folder_name/bin:$PATH
      - echo $PATH
      - echo "verifying sonar-scanner cli installation" && sonar-scanner -h

  pre_build:
    commands:
      - echo "Entered the pre_build phase..."

      - echo "SONAR_HOST - $SONAR_HOST"
      - echo "PROJECT_NAME - $PROJECT_NAME"
      - echo "PROJECT_ROOT_DIRECTORY - $PROJECT_ROOT_DIRECTORY"
      - echo present working directory `pwd`
      - sonar-scanner --version
  build:
    on-failure: ABORT
    commands:
      - echo Build started on `date`
      - echo present working directory `pwd`
      - |
        if [ "$DOWNLOAD_ARTIFACT" = "true" ] ; then
          echo "Skipping SonarScan because download_artifact is true, going to check QG status check"
        else
          if [ "$PROJECT_ROOT_DIRECTORY" != "" ] ; then
            echo "Change directory to $PROJECT_ROOT_DIRECTORY"
            cd $PROJECT_ROOT_DIRECTORY
            ls -l
          fi
          SONAR_BRANCH_SCANNED=${GIT_REPO_BRANCH}
          echo `pwd`
          ls -lart

          # Jfrog server conifguration
          jf c add "${ARTIFACTORY_HOST}" --access-token="${ARTIFACTORY_TOKEN}" --url="${ARTIFACTORY_HOST}" --interactive=false
          jf rt ping
          jf c s

          #Jfrog repo configuraiton
          jf npmc --repo-deploy $ARTIFACTORY_REPO_KEY --repo-resolve $ARTIFACTORY_REPO_KEY
          echo listing files and directories
          ls -lart

          #reinstalling npm dependencies
          echo "Reinstalling packages to resolve cache issues https://docs.npmjs.com/cli/v10/commands/npm-ci"
          jf npm ci
          ls -l node_modules

          # Running unit tests.
          echo "running unit tests with following command $UNIT_TEST_COMMANDS"
          eval $UNIT_TEST_COMMANDS

          # Running Sonar Code Analysis
          echo "Running Sonar Analysis"
          export COMMAND="sonar-scanner  -Dsonar.projectKey="${PROJECT_KEY}" -Dsonar.projectName="${PROJECT_NAME}" -Dsonar.host.url="${SONAR_HOST}" -Dsonar.token="${SONAR_TOKEN}" -Dsonar.branch.name=$GIT_REPO_BRANCH -Dsonar.exclusions=node_modules/**/*,coverage/**/* ${OPTIONAL_ATTRIBUTES}"
          echo $COMMAND
          eval $COMMAND
        fi

      - |
        # Parse and format SonarQube tags from JSON
        APPID=$(echo "$SONAR_PROJECT_TAGS" | jq -r '.AppID')
        ORDER=$(echo "$SONAR_PROJECT_TAGS" | jq -r '.Order')
        TFC_WSID=$(echo "$SONAR_PROJECT_TAGS" | jq -r '.tfc_wsid')
        NOTIFY=$(echo "$SONAR_PROJECT_TAGS" | jq -r '.Notify')
        OWNER=$(echo "$SONAR_PROJECT_TAGS" | jq -r '.Owner')

        # Replace _ with - for SonarQube compliance
        NOTIFY="${NOTIFY//_/-}"
        OWNER="${OWNER//_/-}"

        # Build the tags string
        FORMATTED_TAGS="${APPID},${NOTIFY},${ORDER},${OWNER},${TFC_WSID}"

        # Lowercase and remove non-compliant characters
        FORMATTED_TAGS=$(echo "$FORMATTED_TAGS" | tr '[:upper:]' '[:lower:]')
        FORMATTED_TAGS=$(echo "$FORMATTED_TAGS" | sed 's/[^a-z0-9+-.#,]//g')

        echo "Setting SonarQube project tags: $FORMATTED_TAGS"
        curl -X POST "$SONAR_HOST/api/project_tags/set" \
          -u "$SONAR_TOKEN:" \
          -d "project=$PROJECT_KEY" \
          -d "tags=$FORMATTED_TAGS"

      # checking quality gate status
      - sleep 30;
      - echo "checking SonarQube Quality Gate Status for branch ${SONAR_BRANCH_SCANNED}"
      - curl -s -u "$SONAR_TOKEN:" -G --data-urlencode "branch=$SONAR_BRANCH_SCANNED" --data-urlencode "projectKey=$PROJECT_KEY"  "$SONAR_HOST"/api/qualitygates/project_status > result.json
      - cat result.json
      # Quality scan validation. Build will fail for all enviroments other than Dev if ERROR/WARN found. Build will fail if QG is not attached.
      - |
        if [[ $(jq -r '.projectStatus.status' result.json) != OK ]] ; then
          echo "ERROR/WARN found or there's no QG attached in the Sonar server. Please verify"
          if [[ "${APP_ENVIRONMENT,,}" != "dev" ]] ; then
              export CODEBUILD_BUILD_SUCCEEDING=0 ;
          else
             echo "DEV environment, hence allowed with ERROR/WARN. Check and fix reported issues before moving to prod here - $SONAR_HOST/dashboard?id=$PROJECT_NAME" ;
          fi
        elif [[ $(jq -r '.projectStatus.status' result.json) == None ]] ; then 
          echo "Quality Gate status is None, verify if Quality Gate is attached to sonar project"
          export CODEBUILD_BUILD_SUCCEEDING=0 ;
        fi

  post_build:
    commands:  
      - |
        if [[ $CODEBUILD_BUILD_SUCCEEDING == 0 ]]; then 
            echo "Quality gate status is not success - exiting with error"
            echo " **** check your application vulnerabilities and coverage here - $SONAR_HOST/dashboard?id=$PROJECT_NAME"
            exit 1; 
        else
          echo 'Build Success'
        fi

artifacts:
    files:
      - '**/*'
#adding default cache paths, https://docs.aws.amazon.com/codeartifact/latest/ug/dependency-caching.html
cache:
  paths:
    - '/root/.npm/**/*'